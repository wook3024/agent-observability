global:
  scrape_interval: 15s
  evaluation_interval: 15s

# Remote write receiver is enabled via CLI flag: --web.enable-remote-write-receiver
# No additional configuration needed here for receiving remote writes from OTEL Collector
